# 微信小程序配置

## 需要配置的信息

### 1. appid
文件：`project.config.json`
```json
"appid": "YOUR_WECHAT_APPID"
```
替换为你的小程序 appid，可在微信公众平台获取。

### 2. 云开发环境 ID
文件：`app.js`
```javascript
cloudEnv: 'YOUR_CLOUD_ENV_ID'
```
替换为你的云开发环境 ID，可在微信开发者工具的云开发控制台获取。

## 获取方式

1. **appid**
   - 登录微信公众平台 (https://mp.weixin.qq.com)
   - 开发管理 -> 开发设置 -> AppID

2. **云开发环境 ID**
   - 打开微信开发者工具
   - 点击"云开发"按钮
   - 在云开发控制台查看环境 ID

## 配置步骤

1. 复制 `.env.example` 为 `.env` (可选，用于本地开发)
2. 修改 `project.config.json` 中的 `appid`
3. 修改 `app.js` 中的 `cloudEnv`
4. 如果有云函数，更新 `cloudfunctions/*/config.json` 中的配置
